# Run with `dart run ffigen --config ffigen.yaml`.
name: BatteryPercentageBindings
description: |
  Bindings for `src/battery_percentage.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/battery_percentage_bindings_generated.dart'
headers:
  entry-points:
    - 'src/battery_percentage.h'
  include-directives:
    - 'src/battery_percentage.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
